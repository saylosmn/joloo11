// The tab bar is translucent and absolutely positioned, so screens must pad
// their scroll content by its height. Re-exported here so screens do not each
// depend on expo-router's internal path.
export { useBottomTabBarHeight } from "expo-router/tabs";
