# Runtime dependencies only — keep this list minimal so deploys stay fast.
# Dev/test/tooling lives in requirements-dev.txt.

fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv>=1.0.1

# MongoDB
motor==3.3.1
pymongo==4.6.3

# Models + outbound HTTP (QPay, Google, Telegram)
pydantic>=2.6.4
httpx>=0.27.0

# Google Sign-In: ID token verification (cryptography provides RS256)
pyjwt>=2.10.1
cryptography>=42.0.8
